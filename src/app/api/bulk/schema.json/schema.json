{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Oak Curriculum API Bulk Export Schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["sequenceSlug", "subjectTitle", "sequence", "lessons"],
  "properties": {
    "sequenceSlug": {
      "type": "string",
      "description": "Slug for the subject sequence collection (e.g., 'french-primary')."
    },
    "subjectTitle": {
      "$ref": "#/$defs/subjectTitle"
    },
    "sequence": {
      "type": "array",
      "description": "Ordered list of units for the subject sequence.",
      "items": {
        "$ref": "#/$defs/unit"
      }
    },
    "ks4Options": {
      "type": "array",
      "description": "Available KS4 options for the subject (e.g., exam boards or pathways).",
      "items": {
        "$ref": "#/$defs/examBoard"
      }
    },
    "lessons": {
      "type": "array",
      "description": "Published lessons available for the subject, across units (unitLessons with state != 'published' are excluded).",
      "items": {
        "$ref": "#/$defs/lesson"
      }
    }
  },
  "$defs": {
    "unit": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "unitSlug",
        "unitTitle",
        "canonicalUrl",
        "threads",
        "priorKnowledgeRequirements",
        "nationalCurriculumContent",
        "description",
        "yearSlug",
        "year",
        "keyStageSlug",
        "subjectSlug",
        "unitLessons"
      ],
      "properties": {
        "unitSlug": {
          "type": "string",
          "description": "Slug identifier for the unit."
        },
        "unitTitle": {
          "type": "string",
          "description": "Human-readable unit title."
        },
        "canonicalUrl": {
          "type": "string",
          "format": "uri",
          "description": "Canonical URL for the unit, pointing back to the main Oak National web resource."
        },
        "threads": {
          "type": "array",
          "description": "Thread tags that categorize unit content.",
          "items": {
            "$ref": "#/$defs/thread"
          }
        },
        "examBoard": {
          "$ref": "#/$defs/examBoard"
        },
        "priorKnowledgeRequirements": {
          "type": "array",
          "description": "Prior knowledge statements required for the unit.",
          "items": {
            "type": "string"
          }
        },
        "nationalCurriculumContent": {
          "type": "array",
          "description": "National curriculum references covered by the unit.",
          "items": {
            "type": "string"
          }
        },
        "description": {
          "type": "string",
          "description": "Narrative description of the unit."
        },
        "yearSlug": {
          "$ref": "#/$defs/yearSlug"
        },
        "year": {
          "anyOf": [
            { "type": "integer", "description": "Numeric academic year." },

            {
              "type": "string",
              "enum": ["All years"],
              "description": "This only applies to Physical Education (primary) where swimming is classed as all years."
            }
          ]
        },
        "pathway": {
          "enum": ["GCSE", "Core"],
          "description": "Pathway identifier for the unit (e.g., 'GCSE' or 'Core' for KS4 units)."
        },
        "pathwaySlug": {
          "enum": ["gcse", "core"],
          "description": "Pathway slug for the unit (e.g., 'gcse' or 'core' for KS4 units)."
        },
        "tier": {
          "type": "object",
          "additionalProperties": false,
          "required": ["tierSlug", "tierTitle"],
          "properties": {
            "tierSlug": {
              "enum": ["higher", "foundation"]
            },
            "tierTitle": {
              "enum": ["Higher", "Foundation"]
            }
          }
        },
        "keyStageSlug": {
          "$ref": "#/$defs/keyStageSlug"
        },
        "subjectSlug": {
          "$ref": "#/$defs/subjectSlug"
        },
        "whyThisWhyNow": {
          "type": "string",
          "description": "Rationale for sequence timing and unit placement."
        },
        "unitOptionGroup": {
          "type": "string",
          "description": "If the unit is a unit variant, then this field indicates the option group it belongs to (e.g., 'life-in-a-capital-city-london-cardiff')."
        },
        "examSubjects": {
          "type": "array",
          "description": "Exam subjects relevant to the unit (typically secondary).",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["examSubjectSlug", "examSubjectTitle"],
            "properties": {
              "examSubjectSlug": {
                "$ref": "#/$defs/subjectSlug"
              },
              "examSubjectTitle": {
                "$ref": "#/$defs/subjectTitle"
              }
            }
          }
        },
        "categories": {
          "type": "array",
          "description": "Categorization tags for the unit.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["categoryTitle", "categorySlug"],
            "properties": {
              "categoryTitle": {
                "type": "string",
                "description": "Human-readable category title."
              },
              "categorySlug": {
                "type": "string",
                "description": "Slug identifier for the category."
              }
            }
          }
        },
        "unitLessons": {
          "type": "array",
          "description": "Lessons that belong to the unit in teaching order.",
          "items": {
            "$ref": "#/$defs/unitLesson"
          }
        }
      }
    },
    "thread": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "order", "title"],
      "properties": {
        "slug": {
          "type": "string",
          "description": "Thread slug identifier."
        },
        "order": {
          "type": "integer",
          "description": "Display/order index for the thread."
        },
        "title": {
          "type": "string",
          "description": "Human-readable thread title."
        }
      }
    },
    "unitLesson": {
      "type": "object",
      "additionalProperties": false,
      "required": ["lessonSlug", "lessonTitle", "lessonOrder", "state"],
      "properties": {
        "lessonSlug": {
          "type": "string",
          "description": "Slug identifier for the lesson (must exist in lessons[] when state is 'published')."
        },
        "lessonTitle": {
          "type": "string",
          "description": "Human-readable lesson title."
        },
        "lessonOrder": {
          "type": "integer",
          "description": "Order of the lesson within the unit."
        },
        "state": {
          "type": "string",
          "enum": ["published", "new", "migration"],
          "description": "Publication state for the lesson (only 'published' lessons appear in lessons[])."
        }
      }
    },
    "lesson": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "lessonTitle",
        "lessonSlug",
        "oakUrl",
        "canonicalUrl",
        "unitSlug",
        "unitTitle",
        "subjectSlug",
        "subjectTitle",
        "keyStageSlug",
        "keyStageTitle",
        "lessonKeywords",
        "keyLearningPoints",
        "misconceptionsAndCommonMistakes",
        "pupilLessonOutcome",
        "teacherTips",
        "contentGuidance",
        "downloadsavailable",
        "supervisionLevel"
      ],
      "properties": {
        "lessonTitle": {
          "type": "string",
          "description": "Human-readable lesson title."
        },
        "lessonSlug": {
          "type": "string",
          "description": "Slug identifier for the lesson."
        },
        "oakUrl": {
          "type": "string",
          "format": "uri",
          "description": "Direct URL for the lesson on the Oak National Academy website."
        },
        "canonicalUrl": {
          "type": "string",
          "format": "uri",
          "description": "Canonical URL for the lesson, pointing back to the main Oak National web resource."
        },
        "unitSlug": {
          "type": "string",
          "description": "Slug identifier of the unit this lesson belongs to."
        },
        "unitTitle": {
          "type": "string",
          "description": "Title of the unit this lesson belongs to."
        },
        "subjectSlug": {
          "$ref": "#/$defs/subjectSlug"
        },
        "subjectTitle": {
          "$ref": "#/$defs/subjectTitle"
        },
        "keyStageSlug": {
          "$ref": "#/$defs/keyStageSlug"
        },
        "keyStageTitle": {
          "$ref": "#/$defs/keyStageTitle"
        },
        "lessonKeywords": {
          "type": "array",
          "description": "Keywords used to index or describe the lesson.",
          "items": {
            "$ref": "#/$defs/lessonKeyword"
          }
        },
        "keyLearningPoints": {
          "type": "array",
          "description": "Key learning points for the lesson.",
          "items": {
            "$ref": "#/$defs/keyLearningPoint"
          }
        },
        "misconceptionsAndCommonMistakes": {
          "type": "array",
          "description": "Common misconceptions and guidance responses.",
          "items": {
            "$ref": "#/$defs/misconception"
          }
        },
        "pupilLessonOutcome": {
          "type": "string",
          "description": "Expected lesson outcome for pupils."
        },
        "teacherTips": {
          "type": "array",
          "description": "Tips and guidance for teachers.",
          "items": {
            "$ref": "#/$defs/teacherTip"
          }
        },
        "contentGuidance": {
          "type": ["array", "null"],
          "description": "Additional content guidance; may be null when not provided.",
          "items": {
            "$ref": "#/$defs/contentGuidanceItem"
          }
        },
        "downloadsavailable": {
          "type": "boolean",
          "description": "Whether downloads are available for the lesson."
        },
        "restricted": {
          "type": "boolean",
          "description": "Whether the lesson is restricted or highly restricted, this means access to the assets (such as videos, transcripts, quizzes, etc) is prevented."
        },
        "supervisionLevel": {
          "type": ["string", "null"],
          "description": "Supervision requirement; may be null when not provided."
        },
        "transcript_sentences": {
          "type": ["string", "null"],
          "description": "Transcript in sentence form; may be null when not provided."
        },
        "transcript_vtt": {
          "type": ["string", "null"],
          "description": "Transcript in VTT format; may be null when not provided."
        }
      }
    },
    "lessonKeyword": {
      "type": "object",
      "additionalProperties": false,
      "required": ["keyword", "description"],
      "properties": {
        "keyword": {
          "type": "string",
          "description": "Keyword text."
        },
        "description": {
          "type": "string",
          "description": "Keyword definition or explanation."
        }
      }
    },
    "keyLearningPoint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["keyLearningPoint"],
      "properties": {
        "keyLearningPoint": {
          "type": "string",
          "description": "A single key learning point statement."
        }
      }
    },
    "misconception": {
      "type": "object",
      "additionalProperties": false,
      "required": ["misconception", "response"],
      "properties": {
        "misconception": {
          "type": "string",
          "description": "Misconception statement."
        },
        "response": {
          "type": "string",
          "description": "Recommended response to address the misconception."
        }
      }
    },
    "teacherTip": {
      "type": "object",
      "additionalProperties": false,
      "required": ["teacherTip"],
      "properties": {
        "teacherTip": {
          "type": "string",
          "description": "Tip or guidance for teachers."
        }
      }
    },
    "contentGuidanceItem": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "contentGuidanceArea",
        "supervisionlevel_id",
        "contentGuidanceLabel",
        "contentGuidanceDescription"
      ],
      "properties": {
        "contentGuidanceArea": {
          "type": "string",
          "description": "Content guidance area."
        },
        "supervisionlevel_id": {
          "type": "integer",
          "description": "Supervision level identifier."
        },
        "contentGuidanceLabel": {
          "type": "string",
          "description": "Content guidance label."
        },
        "contentGuidanceDescription": {
          "type": "string",
          "description": "Content guidance description."
        }
      }
    },
    "examBoard": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "title"],
      "properties": {
        "slug": {
          "$ref": "#/$defs/examBoardSlug"
        },
        "title": {
          "$ref": "#/$defs/examBoardTitle"
        },
        "examSubjectTitle": {
          "type": "string",
          "description": "Exam subject title (e.g., 'Computer Science')."
        }
      }
    },
    "ks4Option": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slug", "title"],
      "properties": {
        "slug": {
          "$ref": "#/$defs/examBoardSlug"
        },
        "title": {
          "$ref": "#/$defs/examBoardTitle"
        }
      }
    },
    "examBoardSlug": {
      "type": "string",
      "enum": [
        "aqa",
        "edexcel",
        "eduqas",
        "ocr",
        "wjec",
        "edexcelb",
        "core",
        "gcse"
      ],
      "description": "KS4 option slug (exam board or pathway)."
    },
    "examBoardTitle": {
      "type": "string",
      "enum": [
        "AQA",
        "Edexcel",
        "Eduqas",
        "OCR",
        "WJEC",
        "Edexcel B",
        "Core",
        "GCSE"
      ],
      "description": "KS4 option title (exam board or pathway)."
    },
    "subjectSlug": {
      "type": "string",
      "enum": [
        "art",
        "citizenship",
        "computing",
        "cooking-nutrition",
        "design-technology",
        "english",
        "french",
        "geography",
        "german",
        "history",
        "maths",
        "music",
        "physical-education",
        "religious-education",
        "rshe-pshe",
        "science",
        "biology",
        "chemistry",
        "physics",
        "combined-science",
        "spanish"
      ],
      "description": "Subject slug identifier."
    },
    "yearSlug": {
      "type": "string",
      "enum": [
        "reception",
        "year-1",
        "year-2",
        "year-3",
        "year-4",
        "year-5",
        "year-6",
        "year-7",
        "year-8",
        "year-9",
        "year-10",
        "year-11",
        "year-12",
        "year-13",
        "all-years"
      ],
      "description": "Slug for the academic year or band."
    },
    "keyStageSlug": {
      "type": "string",
      "enum": ["ks1", "ks2", "ks3", "ks4"],
      "description": "Key stage slug identifier."
    },
    "keyStageTitle": {
      "type": "string",
      "enum": ["Key Stage 1", "Key Stage 2", "Key Stage 3", "Key Stage 4"],
      "description": "Key stage title identifier."
    },
    "subjectTitle": {
      "type": "string",
      "enum": [
        "Art and design",
        "Citizenship",
        "Computing",
        "Cooking and nutrition",
        "Design and technology",
        "English",
        "French",
        "Geography",
        "German",
        "History",
        "Maths",
        "Music",
        "Physical education",
        "RSHE (PSHE)",
        "Religious education",
        "Science",
        "Biology",
        "Chemistry",
        "Physics",
        "Combined science",
        "Spanish"
      ],
      "description": "Subject title identifier."
    }
  }
}
